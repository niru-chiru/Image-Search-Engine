import ImageSearch from "../views/containers/imageSearch/imageSearchContainer";

/**
 * Routes to create appliction router Array list
 * @constants
 * @type {object} data
 */
const routes = [{ path: "/", component: ImageSearch, exact: true, id: 1 }];
export default routes;
